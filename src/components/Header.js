import Button from "./Button";
const Header = () => {
  return (
    <div>
      <header className="header">
        <h1>Task Tracker</h1>
        <Button color="green" text="Hello" />
      </header>
    </div>
  );
};

export default Header;
